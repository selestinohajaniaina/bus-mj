import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.bus-mj',
  appName: 'Bus Nakay',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
